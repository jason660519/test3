name: Push Events
on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  show-info:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Info
        run: |
          echo "This is a push event on all the branches"
